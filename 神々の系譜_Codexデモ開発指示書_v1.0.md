# 神々の系譜｜Codexデモ開発指示書 v1.0

作成日：2026年9月14日  
対象：スマートフォンを中心に操作するWebアプリの体験デモ  
公開先：Netlify／コード管理：GitHub

## 1. 今回の依頼

同じリポジトリにある「神々の系譜_アプリ要件仕様書_v1.1.md」と本書を読み、操作できるデモWebアプリを実装してください。計画の説明で終わらず、コード、サンプルデータ、公開設定、動作確認まで進めてください。

全体仕様書は完成版の事業構想・機能・用語を理解する資料です。今回の実装範囲と完了条件には本書を使います。全体仕様書の正式初期版P0をすべて今回のデモへ実装する必要はありません。本書でデモ化した機能は、完成版から削除したものではありません。

既存ファイルを確認してから着手してください。このプロジェクト用のリポジトリで作業し、他のアプリやリポジトリを変更しないでください。

## 2. デモで確認したい体験

利用者が自分に関わる神社と神様を知り、神社を探し、御朱印写真を自分の図鑑に保存し、ミッション達成で徳ポイントを貯め、特典交換へ進む体験を確認します。運営画面で追加したミッションやイベントが利用者画面に反映するところまで動かします。

案内ページだけのサイトではなく、検索、入力、保存、修正、削除、画面遷移ができるアプリにしてください。

## 3. 技術と保存の方針

- React・TypeScript・Viteを基本とし、ルートにpackage.jsonを置きます。npmとpackage-lock.jsonを使います。
- 画面幅375px、390px、768px、1440pxで操作できるレスポンシブ構成にします。
- この段階の保存は同じ端末・同じブラウザ内です。写真のBlobと構造化データをIndexedDB等に保存し、再読み込み後も維持します。写真を大量のBase64としてlocalStorageへ保存しないでください。
- ブラウザデータの削除、別端末、別の公開ドメインでは同じデータを引き継げないことをマイページに説明します。クラウド同期済みとは表示しません。
- マスターデータ、保存処理、写真判定処理、住所照合処理を画面から分け、後から認証・データベース・実AIへ接続できるインターフェースにします。
- 初回のデモ公開に外部AI、決済、会員認証のAPIキーを必須にしません。将来用の秘密鍵をクライアント側のコードへ置かないでください。
- PWA用のmanifest、独自アイコン、standalone表示を用意します。Service Workerを使う場合はアプリ本体の静的資産を中心に扱い、地図タイルの一括保存や個人データの意図しないキャッシュをしません。更新後に古い画面へ戻らない更新案内を用意します。
- インストール方法は端末・ブラウザに応じて案内します。常時GPS、バックグラウンド通知、全機能のオフライン動作は今回の完成条件にしません。

## 4. 画面とデザイン

下部メニューは「ホーム」「探す」「御朱印帳」「ミッション」「マイページ」の5つです。詳細画面には戻る操作を常設し、検索条件と一覧の位置を維持してください。御朱印帳から写真の追加へすぐ進めるようにします。

日本語で、落ち着いた和の雰囲気と、図鑑が埋まる楽しさを両立します。生成り、濃緑、朱色を基調とする案を採用して構いません。大きな宣伝用ヒーローで画面を占有せず、神社カード、直近の記録、図鑑の進捗を最初の画面で見せます。神様の優劣や信仰の深さを点数化しません。既存ゲームのキャラクター・ロゴ・画面を流用しないでください。

通常の操作に必要のない開発用語を画面へ載せず、サンプル処理、端末内保存、交換体験であることは該当機能に短く表示します。

## 5. 実装する機能

### A. ホーム・神社検索・神様図鑑

- 20〜30件程度の寺社サンプルを用意し、名称、読み方、地域、神社・寺院、参拝済み、お気に入りで検索・絞り込みできます。
- 実在寺社の基本情報は公式出典と確認日を付け、確認できない由緒・祭神・写真・受付時間・料金を創作で埋めません。取得が制限される場合は確認済み件数を報告し、残りは架空のサンプルとして区別します。
- 神社詳細からご祭神の名前・読み方・簡単な説明・関係図・関連神社・個人メモへ移動できます。寺院はご本尊として表示します。
- 図鑑は登録数と参拝数を別に数えます。サンプルのみの場合、20件の収集を全国一宮すべての達成のようには表示せず、「デモ収録分」と明記します。
- お気に入りとご祭神ノートを追加・変更・削除し、再読み込み後も維持します。

### B. 地図・外部経路案内

- 地図と一覧を切り替え、表示した寺社のピンから詳細を開けます。地図は公開利用条件を確認したサービスを使い、必要な帰属表示を残します。初期公開に必須の有料契約を追加しない構成を優先してください。
- 利用者が「現在地」を押した時だけ位置情報を求めます。拒否・取得失敗でも地域選択と一覧を利用できます。
- 寺社詳細にGoogleマップの経路案内リンクを設け、対応するiOS環境ではAppleマップも選べるようにします。名称だけでなく所在地・座標を使って目的地を特定します。
- 地図を開いただけで参拝済みにしたりポイントを付けたりしません。登録した住所を出発地として自動送信しません。
- 地図の通信が使えない場合も一覧・寺社詳細を維持し、外部地図リンクへ進めます。

### C. 自分の神社・住所管理

- 現住所と出生地を別々に登録・変更・削除する本人用画面を用意します。デモでは実在個人に結びつかない町域のテスト例を選んで体験する方式を基本とし、実際の住所の入力を求めません。
- デモの住所情報はこのブラウザ内に保存します。本番の認証による本人限定アクセスを実装したと偽らず、共用端末では同じブラウザを開いた人が見られる制約を説明します。住所を公開プロフィール・交流ひろば・URL・ログへ載せません。
- 「公式資料との一致」「複数候補」「詳細確認が必要」「未整備」のケースを操作できるようにします。現在地から最も近い神社を氏神神社として確定させません。
- 公式情報を使う例として、全体仕様書の岡山県神社庁資料にある岡山市北区番町1丁目・2丁目と伊勢神社の対応を、資料を確認したうえで利用できます。出典は https://www.okayama-jinjacho.or.jp/search/16412/ です。
- 公式資料を参照できないケースや架空の分岐例は「サンプル照合」と表示し、公式判定という表示と混在させません。全国一括APIやリアルタイム取得が実装済みであるようには見せません。
- 出生地は「生まれた地域の神社／産土神社の候補」と表示します。テスト住所の変更に応じて結果を更新し、削除後に前の結果が復活しないようにします。

### D. デジタル御朱印帳

- 自分の写真の選択・撮影、プレビュー、寺社と授与日の指定、保存、修正、削除を実装します。JPEG・PNG・WebPを基本とし、HEIC等を扱えない場合は保存失敗のまま放置せず、対応形式への案内を表示します。
- 日付がわからない写真は日付不明で保存できます。撮影日を授与日に自動変換しません。
- 自分の写真は手動で寺社を指定します。デモ用に用意したサンプル画像を選ぶ時は、既知の寺社に自動で振り分ける体験を別に用意し、「サンプルによる自動整理」と表示します。実AIで読み取ったような表示をしません。
- 保存後は寺社別フォルダ、年月別表示、図鑑に即時反映します。寺社を修正すると全表示の関連付けを更新します。同じ寺社の複数写真を保存でき、寺社の収集数は重複加算しません。
- 原画像と縮小表示用の画像を管理し、容量不足・読み込み失敗では入力内容を維持します。同じ写真の再登録は利用者が確認できるようにします。
- 記録の削除後も、すでに発行されたデモポイントを再取得できる状態へ自動的に戻さないでください。

### E. ミッション・徳ポイント・特典交換

- 学習、図鑑登録、参拝体験のミッションを用意し、参加、進捗、達成、ポイント履歴をつなげます。GPSや来訪を実確認していない参拝は「デモ参拝」と表示します。
- デモポイントの初期残高は300、基本ミッション報酬は50・100等とし、初期残高も履歴へ記録します。固定値は設定データへ分離します。
- 同じ達成を二度押ししても二重付与せず、交換連打で残高がマイナスにならないようにします。期間は日本時間で扱い、開始前・終了後のミッションを達成扱いしません。
- 物品と特別体験のサンプル特典を用意し、必要ポイント・残数・日程の確認から交換完了へ進めます。交換すると残高・在庫を更新し、キャンセル可能な予約では一度だけ返還します。
- 実際の発送、支払、神社への予約送信、特別立入の約束はしません。特典一覧と確定画面に「デモ・実際の交換や予約は発生しません」と表示し、配送先を収集しません。

### F. イベント・運営画面・交流

- 運営体験画面からミッション・イベント・特典を作成し、下書き／公開／終了を変更できます。利用者画面への反映も同じブラウザ内で動作させます。
- 運営モードへの切替はデモ用です。本物の管理者認証や、他の利用者への一斉公開を実装済みとは表示しません。デモ用切替を本番権限へ流用しない構成にします。
- イベントは詳細、日程、定員、デモ申込、キャンセル、参加券の表示まで実装します。満席と重複申込を扱います。
- 交流ひろばはサンプル投稿と端末内の投稿・コメント・削除・非表示を体験する範囲です。他ユーザーへの共有や送信は行わず、その範囲を説明します。
- マイページに全デモデータのリセットを用意します。リセット前に写真も消えることを確認し、初期データへ戻します。

## 6. 後続の実装として残す項目

実会員認証、複数端末同期、実住所の保護とサーバー保存、実AIによる写真判定、全国の氏子区域データ整備と更新、ストア課金、実ポイント交換・配送、提携寺社の受付、利用者同士のオンライン共有は後続です。今回のデモが動いても、これらの本番機能が完成したとは報告しないでください。

## 7. Netlifyへの公開設定

ルートのViteプロジェクトとして、NetlifyのBase directoryは未指定、Build commandはnpm run build、Publish directoryはdistに揃えてください。SPAの詳細画面を直接開いた場合や再読み込みした場合も404にならない設定を付けます。

netlify.tomlの基本形：

```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

実際の構成に照らして必要な調整を行い、存在する静的資産が正しく配信されることも確認してください。対応するNode.jsのLTS版を選び、ローカル・Codex・Netlifyの設定を揃えます。package-lock.json、.gitignore、README.mdを納品に含め、node_modules・秘密情報をコミットしません。

Netlifyの連携と公開ボタンの操作は利用者が行います。今回の作業ではコードと公開設定を用意し、可能な範囲で作業ブランチを保存してPR作成まで進めてください。実行環境からできないGitHub操作は、未実施の操作と手順を具体的に報告してください。

## 8. 完了条件

1. npm ciとnpm run buildが成功し、distに起動可能なアプリが生成される。
2. 検索→寺社詳細→ご祭神→御朱印追加→図鑑反映→ミッション→交換の一連の操作がつながる。
3. 登録写真・メモ・お気に入り・ポイント・運営変更が再読み込み後も維持される。
4. 写真の追加・訂正・削除がフォルダと図鑑へ整合して反映する。
5. 住所照合の複数状態と、変更・削除を試せる。住所をURLや交流画面へ出さない。
6. ミッションの重複達成、交換連打、残高不足、満席、キャンセルの再操作で不整合が起きない。
7. 詳細画面の戻る、直接URL、再読み込み、現在地拒否、画像エラーに対応する。
8. スマホ幅で横にはみ出さず、下部メニューや撮影ボタンが内容を隠さない。PCでも操作できる。
9. 実装済み／サンプルで動作／未実装をREADMEで説明し、実際に試した項目と試せていない項目を区別する。

ポイントの一重付与と交換・返還、および住所変更後の結果の整合性には意味のある自動テストを用意します。主要操作をブラウザで確認し、利用可能ならスマホ幅とPC幅のスクリーンショットを残してください。ブラウザ実行が利用できない時は、実機確認済みと書かず、残る確認項目を示してください。

## 9. 最後に報告する内容

- 実装した画面と機能、サンプルの件数
- 実際の処理とサンプル処理の区別
- ビルド・自動テスト・画面確認の結果と未確認事項
- GitHubへ保存したブランチ、PRがある場合はそのURL
- Netlifyへ入力する設定値
- 利用者が最初に試す操作5つ

開発途中でも合理的に決められる細かなデザインや実装方法は判断して進めてください。外部契約・実課金など今回の範囲を変える選択が必要な時は、動作するデモを先に完成させ、その追加項目を報告してください。

## 10. 公開方法の参照資料

以下は2026年9月14日に確認した公式資料です。実装時に設定が変わっている場合は該当箇所を確認してください。

- Codex cloud：https://learn.chatgpt.com/docs/cloud
- Codexの実行環境：https://learn.chatgpt.com/docs/environments/cloud-environment
- Netlifyのリポジトリ連携：https://docs.netlify.com/start/quickstarts/deploy-from-repository/
- Viteのビルド設定とSPAの再読み込み対応：https://docs.netlify.com/build/frameworks/framework-setup-guides/vite/
- Netlifyの公開範囲：https://docs.netlify.com/manage/security/secure-access-to-sites/project-visibility/
